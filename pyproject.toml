[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "blueprinting"
dynamic = ["version"]
description = "Evidence-driven hardware architecture exploration and simulation for distributed AI workloads"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
keywords = [
    "llm",
    "distributed-training",
    "performance-modeling",
    "simulation",
    "deep-learning",
    "gpu",
    "heterogeneous-computing",
    "transformer",
    "parallel-computing",
]
authors = [
    { name = "Reiase", email = "reiase@gmail.com" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: System :: Distributed Computing",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "numpy>=1.20.0",
    "pandas>=1.3.0",
    "rich>=12.0.0",
    "nicegui>=3.15,<4",
    "psutil>=5.9.0",
]

[project.optional-dependencies]
performance-data = [
    "pyarrow>=12.0.0",
]
full = [
    "plotly>=5.0.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "pytest-asyncio>=0.24.0",
]
docs = [
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.0.0",
    "mkdocs-static-i18n[material]>=1.3.1,<2.0.0",
    "mkdocstrings[python]>=0.24.0",
    "pymdown-extensions>=10.21.3,<11.0.0",
]
all = [
    "blueprinting[full,dev,docs,performance-data]",
]

[project.urls]
Homepage = "https://github.com/DeepLink-org/Blueprinting"
Documentation = "https://deeplink-org.github.io/Blueprinting/"
Repository = "https://github.com/DeepLink-org/Blueprinting.git"
Issues = "https://github.com/DeepLink-org/Blueprinting/issues"
Changelog = "https://github.com/DeepLink-org/Blueprinting/releases"

[project.scripts]
blueprinting = "blueprinting.cli:main"
blueprinting-workbench = "blueprinting.workbench.nicegui_app:main"

# ============================================================================
# Hatch Configuration
# ============================================================================

[tool.hatch.version]
source = "vcs"

[tool.hatch.version.raw-options]
version_scheme = "no-guess-dev"

[tool.hatch.build.hooks.vcs]
version-file = "src/blueprinting/__about__.py"

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/data",
    "/tests",
]
exclude = [
    "/.github",
    	"/docs",
    	"/examples",
    	"/data/evidence",
]

[tool.hatch.build.targets.wheel]
packages = ["src/blueprinting", "src/calculon"]

[tool.hatch.build.targets.wheel.force-include]
"data/models" = "blueprinting/presets/models"
"data/systems" = "blueprinting/presets/systems"
"data/validation/vidur/phi2_a100_tp1" = "blueprinting/presets/evidence/vidur/phi2_a100_tp1"

[tool.hatch.envs.default]
features = ["dev", "full"]

[tool.hatch.envs.default.scripts]
test = "pytest {args:tests}"
test-cov = "pytest --cov=blueprinting --cov-report=term-missing {args:tests}"
baseline-regression = "pytest -m baseline_regression {args:tests/regression}"
lint = "ruff check src/ tests/"
fmt = "ruff format src/ tests/"

[tool.hatch.envs.docs]
features = ["docs"]

[tool.hatch.envs.docs.scripts]
check = "python scripts/check_docs_i18n.py"
build = "python scripts/check_docs_i18n.py && mkdocs build --strict"
serve = "mkdocs serve"

# ============================================================================
# Ruff Configuration
# ============================================================================

[tool.ruff]
line-length = 120
target-version = "py310"
src = ["src", "tests"]
exclude = [
    "src/calculon/*",
    "scripts/*",
    ".git",
    "__pycache__",
    "*.egg-info",
]

[tool.ruff.format]
docstring-code-format = true
quote-style = "double"
indent-style = "space"

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "SIM",    # flake8-simplify
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "B008",   # do not perform function calls in argument defaults
    "B905",   # zip strict
    "SIM108", # ternary operator
]

[tool.ruff.lint.isort]
known-first-party = ["blueprinting", "calculon"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B", "SIM"]
"__init__.py" = ["F401"]

# ============================================================================
# Pytest Configuration
# ============================================================================

[tool.pytest.ini_options]
minversion = "7.0"
asyncio_mode = "auto"
addopts = [
    "-ra",
    "-q",
    "--strict-markers",
    "--doctest-modules",
    "--durations=10",
]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "baseline_regression: pinned external-baseline regression gates required in CI",
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

# ============================================================================
# Coverage Configuration
# ============================================================================

[tool.coverage.run]
source_pkgs = ["blueprinting"]
branch = true
parallel = true
omit = [
    "src/blueprinting/__about__.py",
    "src/calculon/*",
]

[tool.coverage.paths]
blueprinting = ["src/blueprinting"]
tests = ["tests"]

[tool.coverage.report]
exclude_lines = [
    "no cov",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "pass",
    "@abstractmethod",
]
show_missing = true

# ============================================================================
# MyPy Configuration (Optional)
# ============================================================================

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = [
    "src/calculon/",
    "tests/",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
]
