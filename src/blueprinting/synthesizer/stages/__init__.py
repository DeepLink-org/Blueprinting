"""Discoverable homes for the five canonical formal-representation stages.

Each stage owns its canonical declarations in ``ir.py`` and exposes the
transformations that produce it in ``passes.py``. The package intentionally
does not import every stage eagerly.
"""
